[meta]
  dir = "/var/influx/meta"

[data]
  dir = "/var/influx/data"
  wal-dir = "/var/influx/wal"

[admin]
  https-enabled = true
  https-certificate = "/etc/influx.d/ssl/influx.crt"

[http]
  https-enabled = true
  https-certificate = "/etc/influx.d/ssl/influx.crt"
